% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnSegmentPlot.R
\name{.cn_seg_add_gene_labels}
\alias{.cn_seg_add_gene_labels}
\title{Label genes on a copy number segment plot}
\usage{
.cn_seg_add_gene_labels(
  p,
  genes,
  id.col,
  sigs,
  seqstart,
  totlen,
  seq.names,
  label.size
)
}
\arguments{
\item{p}{A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object to add labels to.}

\item{genes}{A \code{GRanges} of gene coordinates.}

\item{id.col}{Name of the metadata column in \code{genes} holding the label. If
\code{NULL}, \code{names(genes)} is used.}

\item{sigs}{The \code{seg.signals} data.frame (\code{chrom}, \code{loc.start}, \code{loc.end},
\code{seg.mean} columns).}

\item{seqstart}{Named numeric vector of per-chromosome genome-wide offsets.}

\item{totlen}{Total genome length (sum of plotted chromosome lengths).}

\item{seq.names}{Character vector of chromosome names being plotted.}

\item{label.size}{Text size of the gene labels.}
}
\value{
The input \code{ggplot2::ggplot()} object, with gene labels added.
}
\description{
Internal helper for \code{\link[=cnSegmentPlot]{cnSegmentPlot()}}. For each gene overlapping a called
segment, a text label is placed at the gene's genome position and the
overlapping segment's mean signal. Genes overlapping multiple segments are
labeled at the segment with the largest overlap.
}
\author{
Jared Andrews
}
\keyword{internal}
