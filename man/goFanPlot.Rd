% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/goFanPlot.R
\name{goFanPlot}
\alias{goFanPlot}
\title{Create an interactive GO enrichment sunburst ("fan") plot}
\usage{
goFanPlot(
  data,
  org = "org.Hs.eg.db",
  term.id = "ID",
  fill = "qvalue",
  sub_rect = NULL,
  onto = c("BP", "CC", "MF"),
  go.annotation.level.cutoff = 4,
  filter.nodes.by.edge.number = 2,
  fill.na.by.0 = TRUE,
  must.keep = NULL,
  only.keep = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame of enriched GO terms. Must contain the \code{term.id}
column of GO identifiers and the \code{fill} column.}

\item{org}{Either an \code{OrgDb} object or the name of an installed organism
annotation package (e.g. \code{"org.Hs.eg.db"}, \code{"org.Mm.eg.db"},
\code{"org.Dr.eg.db"}). Defaults to \code{"org.Hs.eg.db"}.}

\item{term.id}{The name of the column in \code{data} holding the GO IDs
(default \code{"ID"}).}

\item{fill}{The name of the numeric column in \code{data} used to set the segment
fill colours (default \code{"qvalue"}).}

\item{sub_rect}{Optional name of a numeric column in \code{data} used to draw a
proportional sub-rectangle inside each segment (a value in \verb{[0, 1]}; count
columns are converted to a proportion). \code{NULL} or \code{""} disables it.}

\item{onto}{The GO ontology category of the supplied IDs. One of \code{"BP"}
(biological process), \code{"CC"} (cellular component), or \code{"MF"} (molecular
function).}

\item{go.annotation.level.cutoff}{Numeric cutoff for the GO annotation levels
passed to \code{\link[GOfan:sunburstGO]{GOfan::sunburstGO()}} as \code{GO_annotation_level_cutoff}
(default \code{4}).}

\item{filter.nodes.by.edge.number}{Filter the sub-graphs by their edge number
(passed as \code{filterNodesByEdgeNumber}, default \code{2}).}

\item{fill.na.by.0}{Logical; fill \code{NA} values in the colour column with \code{0}
(passed as \code{fillNAby0}, default \code{TRUE}).}

\item{must.keep}{Optional character vector of GO terms that must be kept
(passed as \code{mustkeep}).}

\item{only.keep}{Optional character vector of GO terms; only branches
containing these terms are kept (passed as \code{onlyKeep}).}

\item{...}{Further arguments passed to \code{\link[GOfan:sunburstGO]{GOfan::sunburstGO()}}.}
}
\value{
A \code{\link[plotly:plot_ly]{plotly::plot_ly()}} object containing the interactive sunburst plot.
}
\description{
Builds a Gene Ontology (GO) enrichment sunburst plot from an enrichment
results table and returns an interactive \code{plotly} figure. The plot is
produced with \code{\link[GOfan:sunburstGO]{GOfan::sunburstGO()}}, which converts the GO directed acyclic
graph (DAG) into a clean, circular (sunburst / fan) representation where each
ring corresponds to a hierarchy level and each segment represents a GO term.
}
\details{
The input \code{data} must contain a column of GO identifiers (e.g.
\code{"GO:0006955"}) and at least one numeric column to map onto the segment fill
colour (for example a q-value or \code{-log10} p-value). \code{\link[GOfan:sunburstGO]{GOfan::sunburstGO()}}
resolves the hierarchical relationships among the supplied GO terms using the
organism annotation database given in \code{org}, so the appropriate \code{OrgDb}
package (e.g. \code{org.Hs.eg.db} for human) must be installed.

\code{GOfan::sunburstGO()} is rendered with \code{plotBy = "plotly"} so the returned
figure is genuinely interactive: hovering a segment reveals its label and
clicking a segment zooms into that GO term and its offspring.
}
\examples{
library(sciVizModules)
data(example_enrichment)
# Requires the 'GOfan' package and the relevant OrgDb (e.g. org.Hs.eg.db).
if (requireNamespace("GOfan", quietly = TRUE) &&
    requireNamespace("org.Hs.eg.db", quietly = TRUE)) {
    fig <- goFanPlot(example_enrichment,
        org = "org.Hs.eg.db",
        term.id = "ID", fill = "qvalue", onto = "BP"
    )
    if (interactive()) fig
}
}
\seealso{
\code{\link[GOfan:sunburstGO]{GOfan::sunburstGO()}}, \code{\link[=goFanPlotInputsUI]{goFanPlotInputsUI()}},
\code{\link[=goFanPlotServer]{goFanPlotServer()}}, \code{\link[=goFanPlotApp]{goFanPlotApp()}}
}
\author{
Jacob Martin, Jared Andrews
}
