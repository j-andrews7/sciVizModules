% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maPlot_module_app.R
\name{maPlotApp}
\alias{maPlotApp}
\title{Create a standalone Shiny app for the maPlot module}
\usage{
maPlotApp(data_list = NULL)
}
\arguments{
\item{data_list}{An optional named list of data frames. If \code{NULL} (the default),
\code{list("airway_deseq2" = airway_deseq2)} is used as example data. Each data frame
must contain a mean abundance (e.g., baseMean), effect size (e.g., log2FoldChange),
and significance (e.g., padj) columns.}
}
\value{
A Shiny app object.
}
\description{
This function generates a Shiny application with modular MA plot components.
The app features a \strong{Data Import} section for uploading Excel spreadsheets,
a \strong{Data Table} for viewing and editing the active dataset, and a \strong{Plot} area
for configuring and displaying an interactive MA plot.
}
\details{
When \code{data_list} is not provided (or \code{NULL}), the app launches with
\code{airway_deseq2} as an example dataset. Uploaded Excel files are added
to the available datasets and can be selected for plotting. If an uploaded
file shares a name with an existing dataset, the existing one is overwritten
with a warning.
}
\examples{
library(sciVizModules)
# Launch with default example data:
app <- maPlotApp()
if (interactive()) runApp(app)

# Launch with custom data:
data(airway_deseq2)
app2 <- maPlotApp(list("airway" = airway_deseq2))
if (interactive()) runApp(app2)
}
\seealso{
\code{\link[=maPlotInputsUI]{maPlotInputsUI()}}, \code{\link[=maPlotOutputUI]{maPlotOutputUI()}},
\code{\link[=maPlotServer]{maPlotServer()}}, \link{airway_deseq2}
}
\author{
Jared Andrews
}
