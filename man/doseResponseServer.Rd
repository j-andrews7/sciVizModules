% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doseResponse_module_server.R
\name{doseResponseServer}
\alias{doseResponseServer}
\title{Server logic for the dose-response module}
\usage{
doseResponseServer(
  id,
  data,
  hide.inputs = NULL,
  hide.tabs = NULL,
  defaults = NULL
)
}
\arguments{
\item{id}{The ID for the Shiny module.}

\item{data}{A \code{reactive} returning the dose-response data frame.}

\item{hide.inputs}{Additional input IDs to hide, on top of the module's
built-in dose-response hidden set.}

\item{hide.tabs}{Additional tab names to hide, on top of the module's
built-in dose-response hidden set.}

\item{defaults}{A named list of default values. Merged over the module's
dose-response defaults (user values win).}
}
\value{
The value returned by \code{\link[VizModules:dittoViz_scatterPlotServer]{VizModules::dittoViz_scatterPlotServer()}}.
}
\description{
Dose-response curve fitting built on the VizModules scatter plot module
(\code{\link[VizModules:dittoViz_scatterPlotServer]{VizModules::dittoViz_scatterPlotServer()}}). The module registers a
\pkg{drc} \code{"drm"} model backend and delegates to the scatter plot server
with dose-response defaults (dose on a log10 x-axis, response on the y-axis,
and a \code{drc} log-logistic model drawn as the fitted curve). Tabs and inputs
that are not relevant to a single dose-response curve are hidden.
}
\seealso{
\code{\link[=doseResponseInputsUI]{doseResponseInputsUI()}},
\code{\link[=doseResponseOutputUI]{doseResponseOutputUI()}}, \code{\link[=doseResponseApp]{doseResponseApp()}}
}
\author{
Jacob Martin
}
