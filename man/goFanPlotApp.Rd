% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/goFanPlot_module_app.R
\name{goFanPlotApp}
\alias{goFanPlotApp}
\title{Create a standalone Shiny app for the goFanPlot module}
\usage{
goFanPlotApp(data_list = NULL)
}
\arguments{
\item{data_list}{An optional named list of enrichment results data frames. If
\code{NULL} (the default), \code{list("example_enrichment" = example_enrichment)} is
used as example data. Each data frame should contain a column of GO
identifiers and a numeric column (e.g. \code{qvalue}) to colour by.}
}
\value{
A Shiny app object.
}
\description{
This function generates a Shiny application with modular GO enrichment
sunburst ("fan") plot components. The app features a \strong{Data Import} section
for uploading data files, a \strong{Data Table} for filtering the active dataset,
and a \strong{Plot} area for configuring and displaying an interactive sunburst
plot built on \code{\link[GOfan:sunburstGO]{GOfan::sunburstGO()}}.
}
\details{
When \code{data_list} is not provided (or \code{NULL}), the app launches with the
bundled \code{example_enrichment} dataset. Uploaded files are added to the
available datasets and can be selected for plotting. If an uploaded file
shares a name with an existing dataset, the existing one is overwritten with
a warning.

This is a convenience wrapper around \code{\link[VizModules:createModuleApp]{VizModules::createModuleApp()}}.
}
\note{
Rendering the plot requires the \pkg{GOfan} package and the relevant
organism annotation (\code{OrgDb}) package (e.g. \code{org.Hs.eg.db}) to be installed.
}
\examples{
library(sciVizModules)
# Launch with default example data:
app <- goFanPlotApp()
if (interactive()) shiny::runApp(app)

# Launch with custom data:
data(example_enrichment)
app2 <- goFanPlotApp(list("enrichment" = example_enrichment))
if (interactive()) shiny::runApp(app2)
}
\seealso{
\code{\link[=goFanPlotInputsUI]{goFanPlotInputsUI()}}, \code{\link[=goFanPlotOutputUI]{goFanPlotOutputUI()}},
\code{\link[=goFanPlotServer]{goFanPlotServer()}}, \link{example_enrichment}
}
\author{
Jacob Martin, Jared Andrews
}
