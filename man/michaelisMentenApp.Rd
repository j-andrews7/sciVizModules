% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/michaelisMentenPlot_module_app.R
\name{michaelisMentenApp}
\alias{michaelisMentenApp}
\title{Create a standalone Shiny app for the michaelisMenten module}
\usage{
michaelisMentenApp(
  data = mm_kinetics,
  model = mm_kinetics_line,
  stats = mm_kinetics_fit,
  defaults = NULL,
  title = "Modular Michaelis-Menten Plot"
)
}
\arguments{
\item{data}{The plotting data frame of observed points. Defaults to the
bundled \link{mm_kinetics}.}

\item{model}{A data frame of fitted-line coordinates (e.g. \code{mml}) sharing the
same x/y columns as \code{data}. Defaults to the bundled \link{mm_kinetics_line}.}

\item{stats}{Optional fitted model (e.g. an \code{nls} fit of
\code{v ~ Vm * S / (K + S)}) or named coefficients from which K and Vmax are
extracted for annotation. Defaults to the bundled \link{mm_kinetics_fit};
pass \code{NULL} to disable the annotation.}

\item{defaults}{An optional named list of default input values.}

\item{title}{The app title.}
}
\value{
A Shiny app object.
}
\description{
This function generates a Shiny application with the modular
Michaelis-Menten plotting components: an inputs panel for configuring the
plot and an output panel showing the interactive \code{plotly} figure.
}
\details{
Unlike the upload-driven \code{\link[VizModules:createModuleApp]{VizModules::createModuleApp()}} wrapper, this app
takes the three data inputs directly because the module needs more than a
single data frame: the observed data, the fitted line, and (optionally) the
stats object used for K / Vmax annotations.

When called with no arguments, the app launches with the bundled example
kinetics data (\link{mm_kinetics}, \link{mm_kinetics_line}, and \link{mm_kinetics_fit}).
}
\examples{
library(sciVizModules)
# Launch with the bundled example kinetics data:
app <- michaelisMentenApp()
if (interactive()) shiny::runApp(app)

# Launch with custom data:
if (requireNamespace("drc", quietly = TRUE)) {
    library(drc)
    mm_model <- drm(v ~ S, data = mm_kinetics, fct = MM.2())
    mml <- data.frame(S = seq(min(mm_kinetics$S), max(mm_kinetics$S), length.out = 100))
    mml$v <- predict(mm_model, newdata = mml)
    stats <- nls(v ~ Vm * S / (K + S), data = mm_kinetics,
        start = list(K = max(mm_kinetics$v) / 2, Vm = max(mm_kinetics$v)))
    app2 <- michaelisMentenApp(mm_kinetics, mml, stats)
    if (interactive()) shiny::runApp(app2)
}
}
\seealso{
\code{\link[=michaelisMentenInputsUI]{michaelisMentenInputsUI()}},
\code{\link[=michaelisMentenOutputUI]{michaelisMentenOutputUI()}},
\code{\link[=michaelisMentenServer]{michaelisMentenServer()}}, \code{\link[=michaelisMentenPlot]{michaelisMentenPlot()}}
}
\author{
Jacob Martin
}
