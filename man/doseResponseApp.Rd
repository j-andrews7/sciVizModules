% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doseResponse_module_app.R
\name{doseResponseApp}
\alias{doseResponseApp}
\title{Create a standalone Shiny app for the dose-response module}
\usage{
doseResponseApp(data_list = NULL, defaults = NULL)
}
\arguments{
\item{data_list}{An optional named list of dose-response data frames. If
\code{NULL} (the default), \code{list("dose_response" = dose_response)} is used.}

\item{defaults}{A named list of default values, merged over the module's
dose-response defaults (user values win).}
}
\value{
A Shiny app object.
}
\description{
Convenience wrapper around \code{\link[VizModules:createModuleApp]{VizModules::createModuleApp()}} that launches the
dose-response module: a scatter plot of response versus dose (log10 x-axis)
with a \strong{drc} log-logistic model fitted as the curve. Irrelevant scatter
plot tabs and inputs are hidden.
}
\details{
When \code{data_list} is not provided, the app launches with the bundled
\link{dose_response} example data.
}
\examples{
library(sciVizModules)
# Launch with the bundled example dose-response data:
app <- doseResponseApp()
if (interactive()) shiny::runApp(app)
}
\seealso{
\code{\link[=doseResponseInputsUI]{doseResponseInputsUI()}},
\code{\link[=doseResponseOutputUI]{doseResponseOutputUI()}}, \code{\link[=doseResponseServer]{doseResponseServer()}},
\link{dose_response}
}
\author{
Jacob Martin
}
