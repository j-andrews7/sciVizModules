% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichmentDotPlot_module_app.R
\name{enrichmentDotPlotApp}
\alias{enrichmentDotPlotApp}
\title{Create a standalone Shiny app for the enrichmentDotPlot module}
\usage{
enrichmentDotPlotApp(data_list = NULL)
}
\arguments{
\item{data_list}{An optional named list of enrichment results data frames. If
\code{NULL} (the default), \code{list("example_enrichment" = example_enrichment)} is
used as example data. Each data frame should contain enrichment terms and,
ideally, \code{GeneRatio} and p-value columns.}
}
\value{
A Shiny app object.
}
\description{
This function generates a Shiny application with modular enrichment dot-plot
components. The app features a \strong{Data Import} section for uploading data
files, a \strong{Data Table} for filtering the active dataset, and a \strong{Plot} area
for configuring and displaying an interactive enrichment dot plot built on
\code{\link[plotthis:DotPlot]{plotthis::DotPlot()}}.
}
\details{
When \code{data_list} is not provided (or \code{NULL}), the app launches with the
bundled \code{example_enrichment} dataset. Uploaded files are added to the
available datasets and can be selected for plotting. If an uploaded file
shares a name with an existing dataset, the existing one is overwritten with
a warning.

This is a convenience wrapper around \code{\link[VizModules:createModuleApp]{VizModules::createModuleApp()}}.
}
\examples{
library(sciVizModules)
# Launch with default example data:
app <- enrichmentDotPlotApp()
if (interactive()) shiny::runApp(app)

# Launch with custom data:
data(example_enrichment)
app2 <- enrichmentDotPlotApp(list("enrichment" = example_enrichment))
if (interactive()) shiny::runApp(app2)
}
\seealso{
\code{\link[=enrichmentDotPlotInputsUI]{enrichmentDotPlotInputsUI()}}, \code{\link[=enrichmentDotPlotOutputUI]{enrichmentDotPlotOutputUI()}},
\code{\link[=enrichmentDotPlotServer]{enrichmentDotPlotServer()}}, \link{example_enrichment}
}
\author{
Jacob Martin, Jared Andrews
}
