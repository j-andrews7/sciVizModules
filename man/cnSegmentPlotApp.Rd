% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnSegmentPlot_module_app.R
\name{cnSegmentPlotApp}
\alias{cnSegmentPlotApp}
\title{Create a standalone Shiny app for the cnSegmentPlot module}
\usage{
cnSegmentPlotApp(
  seg = example_cn_segment,
  defaults = NULL,
  title = "Array Copy Number Segments"
)
}
\arguments{
\item{seg}{A \code{CNSegment} object, as returned by \code{\link[sesame:cnSegmentation]{sesame::cnSegmentation()}}.
Defaults to the bundled \link{example_cn_segment}.}

\item{defaults}{An optional named list of default input values.}

\item{title}{The app title.}
}
\value{
A Shiny app object.
}
\description{
This function generates a Shiny application with the modular copy number
segment plotting components: an inputs panel for configuring the plot and
an output panel showing the interactive \code{plotly} figure.
}
\details{
Unlike the upload-driven \code{\link[VizModules:createModuleApp]{VizModules::createModuleApp()}} wrapper, this app
takes the \code{CNSegment} object directly rather than a single data frame. Gene
labels and centromere positions are derived from the object's \code{genomeInfo}
(\code{genomeInfo$genes} and \code{genomeInfo$cytoBand}, respectively).

When called with no arguments, the app launches with the bundled
\link{example_cn_segment} object; a curated set of cancer genes is labeled
initially.
}
\examples{
library(sciVizModules)
# Launch with the bundled example data:
app <- cnSegmentPlotApp()
if (interactive()) shiny::runApp(app)
}
\seealso{
\code{\link[=cnSegmentPlotInputsUI]{cnSegmentPlotInputsUI()}},
\code{\link[=cnSegmentPlotOutputUI]{cnSegmentPlotOutputUI()}},
\code{\link[=cnSegmentPlotServer]{cnSegmentPlotServer()}}, \code{\link[=cnSegmentPlot]{cnSegmentPlot()}}
}
\author{
Jared Andrews
}
