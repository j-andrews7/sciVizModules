% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dittoDimPlot_module_ui.R
\name{dittoDimPlotInputsUI}
\alias{dittoDimPlotInputsUI}
\title{Input UI components for the dittoDimPlot module}
\usage{
dittoDimPlotInputsUI(
  id,
  data,
  defaults = NULL,
  title = "DimPlot Settings",
  columns = 2
)
}
\arguments{
\item{id}{The ID for the Shiny module.}

\item{data}{A \code{SingleCellExperiment}, \code{Seurat}, or \code{SummarizedExperiment} object
used to populate the input choices.}

\item{defaults}{A named list of default values for the inputs.}

\item{title}{An optional title for the UI grid.}

\item{columns}{Number of columns for the UI grid.}
}
\value{
A Shiny tagList containing the UI elements
}
\description{
This should be placed in the UI where the inputs should be shown, with an \code{id}
that matches the \code{id} used in the \code{dittoDimPlotServer()} and
\code{dittoDimPlotOutputUI()} functions.
}
\details{
The user inputs for this module are separated from the outputs to allow for
more flexible UI design.

The inputs are organized into tabs via the \code{\link[VizModules:organize_inputs]{VizModules::organize_inputs()}} function,
with \code{columns} controlling the number of columns in each tab's grid.

This module wraps \code{\link[dittoSeq:dittoDimPlot]{dittoSeq::dittoDimPlot()}}, which displays gene expression or
metadata overlaid on a dimensionality reduction (e.g. UMAP, t-SNE, PCA)
embedding stored in a \code{SingleCellExperiment}, \code{Seurat}, or \code{SummarizedExperiment}
object. The resulting \code{ggplot} is converted to an interactive \code{plotly} figure.
}
\section{Plot parameters and defaults}{

The following parameters can be accessed via UI inputs and/or the \code{defaults} argument:
\itemize{
\item \code{var} - Gene or metadata to color by (default: first metadata column)
\item \code{reduction.use} - Dimensionality reduction to plot (default: auto-detected,
priority UMAP > t-SNE > PCA)
\item \code{dim.1}, \code{dim.2} - Which dimensions to plot on x/y (default: 1 and 2)
\item \code{shape.by} - Discrete metadata mapped to point shape (default: none)
\item \code{split.by} - Discrete metadata to facet by (default: none)
\item \code{size} - Point size (default: 1)
\item \code{opacity} - Point opacity (default: 1)
\item \code{order} - Point plotting order (default: "unordered")
\item \code{do.label} - Draw cluster/group labels (default: FALSE)
\item \code{labels.size} - Text size of the group labels (default: 5)
\item \code{labels.highlight} - White box behind group labels (default: TRUE)
\item \code{do.ellipse} - Draw grouping ellipses (default: FALSE)
\item \code{do.contour} - Overlay kernel-density contour lines (default: FALSE)
\item \code{min.color}, \code{max.color} - Colors for continuous color scales
\item \code{palette.selection} - Colors for discrete groups (multiColorPicker)
}
}

\examples{
library(sciVizModules)
data(example_sce)
dittoDimPlotInputsUI("dittoDimPlot", example_sce)
}
\seealso{
\code{\link[dittoSeq:dittoDimPlot]{dittoSeq::dittoDimPlot()}}, \code{\link[VizModules:organize_inputs]{VizModules::organize_inputs()}},
\code{\link[=dittoDimPlotOutputUI]{dittoDimPlotOutputUI()}}, \code{\link[=dittoDimPlotServer]{dittoDimPlotServer()}},
\code{\link[=dittoDimPlotApp]{dittoDimPlotApp()}}
}
\author{
Jared Andrews
}
