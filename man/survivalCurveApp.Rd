% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survivalCurve_module_app.R
\name{survivalCurveApp}
\alias{survivalCurveApp}
\title{Create a standalone Shiny app for the survivalCurve module}
\usage{
survivalCurveApp(data_list = NULL)
}
\arguments{
\item{data_list}{An optional named list of data frames. If \code{NULL} (the default),
\code{list("survival_lung" = survival_lung)} is used as example data. Each data frame
must contain a numeric follow-up time column and an event/status column.}
}
\value{
A Shiny app object.
}
\description{
This function generates a Shiny application with modular survival curve
components. The app features a \strong{Data Import} section for uploading data
files, a \strong{Data Table} for filtering the active dataset, and a \strong{Plot} area
for configuring and displaying an interactive Kaplan-Meier survival curve.
}
\details{
When \code{data_list} is not provided (or \code{NULL}), the app launches with
\code{survival_lung} as an example dataset. Uploaded files are added to the
available datasets and can be selected for plotting.

This is a convenience wrapper around \code{\link[VizModules:createModuleApp]{VizModules::createModuleApp()}}.
}
\examples{
library(sciVizModules)
# Launch with default example data:
app <- survivalCurveApp()
if (interactive()) shiny::runApp(app)

# Launch with custom data:
data(survival_lung)
app2 <- survivalCurveApp(list("lung" = survival_lung))
if (interactive()) shiny::runApp(app2)
}
\seealso{
\code{\link[=survivalCurveInputsUI]{survivalCurveInputsUI()}}, \code{\link[=survivalCurveOutputUI]{survivalCurveOutputUI()}},
\code{\link[=survivalCurveServer]{survivalCurveServer()}}, \code{\link{survival_lung}}
}
\author{
Jared Andrews
}
