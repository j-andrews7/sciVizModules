% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcanoPlot_module_ui.R
\name{volcanoPlotInputsUI}
\alias{volcanoPlotInputsUI}
\title{Input UI components for the volcanoPlot module}
\usage{
volcanoPlotInputsUI(
  id,
  data,
  defaults = NULL,
  title = "Volcano Settings",
  columns = 2
)
}
\arguments{
\item{id}{The ID for the Shiny module.}

\item{data}{The data frame used for plot generation.}

\item{defaults}{A named list of default values for the inputs.}

\item{title}{An optional title for the UI grid.}

\item{columns}{Number of columns for the UI grid.}
}
\value{
A Shiny tagList containing the UI elements
}
\description{
This should be placed in the UI where the inputs should be shown, with an \code{id}
that matches the \code{id} used in the \code{volcanoPlotServer()} and \code{volcanoPlotOutputUI()} functions.
}
\details{
The user inputs for this module are separated from the outputs to allow for
more flexible UI design.

The inputs will automatically be organized into a grid layout via the \code{\link[VizModules:organize_inputs]{VizModules::organize_inputs()}} function,
with \code{columns} controlling the number of columns in the grid.

Defaults can be set for each input by providing a named list of values to the \code{defaults} argument.
This module wraps \code{\link[VizModules:dittoViz_scatterPlotInputsUI]{VizModules::dittoViz_scatterPlotInputsUI()}} and adds volcano-specific controls.
The base scatter plot UI components and their input IDs (e.g. \code{x.by}, \code{y.by},
\code{color.by}, \code{hover.data}) are documented in \code{\link[VizModules:dittoViz_scatterPlotInputsUI]{VizModules::dittoViz_scatterPlotInputsUI()}};
only the volcano-specific additions are described here.

Additional inputs specific to volcano plots are added to control significance thresholds and colors:
\itemize{
\item \code{sig.thresh}: Significance threshold (default 0.05)
\item \code{fc.thresh}: Log2 fold change threshold (default 0)
\item \code{volcano.colors}: A multiColorPicker for Up/Down/n.s. group colors
(defaults: Up="red", Down="blue", n.s.="lightgray")
}
}
\section{Plot parameters and defaults}{

The following parameters can be accessed via UI inputs and/or the \code{defaults} argument:
\itemize{
\item \code{x.by} - X-axis variable (auto-detected from effect size columns: log2FoldChange, LFC, logFC)
\item \code{y.by} - Y-axis variable (auto-detected from significance columns: padj, pval, adj.p, svalue, FDR, p)
\item \code{color.by} - Coloring variable (default: "group", auto-generated from thresholds)
\item \code{y.adj.fxn} - Y adjustment function (default: "neg_log10" for -log10(p-value))
\item \code{show.others} - Show others (default: FALSE)
\item \code{hover.data} - Hover data columns (default: c("symbol", x.by, y.by))
\item \code{sig.thresh} - Significance threshold (UI: "Significance Threshold", default: 0.05)
\item \code{fc.thresh} - Log2 fold change threshold (UI: "LFC Threshold (log2)", default: 0)
\item All other \code{\link[dittoViz:scatterPlot]{dittoViz::scatterPlot()}} parameters are also available via the wrapped UI
}
}

\section{Parameters controlling additional functionality}{

The following parameters implementing volcano-specific features are also available:
\itemize{
\item \code{volcano.colors} - Named color vector for Up/Down/n.s. groups (UI: "Group Colors" multiColorPicker)
\item \code{group} - Auto-generated grouping column based on sig.thresh and fc.thresh
}
}

\examples{
library(sciVizModules)
data(airway_deseq2)
volcanoPlotInputsUI("volcanoPlot", airway_deseq2)
}
\seealso{
\code{\link[dittoViz:scatterPlot]{dittoViz::scatterPlot()}}, \code{\link[VizModules:dittoViz_scatterPlotInputsUI]{VizModules::dittoViz_scatterPlotInputsUI()}},
\code{\link[=volcanoPlotOutputUI]{volcanoPlotOutputUI()}}, \code{\link[=volcanoPlotServer]{volcanoPlotServer()}},
\code{\link[=volcanoPlotApp]{volcanoPlotApp()}}
}
\author{
Jacob Martin
}
