% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/michaelisMentenPlot_helpers.R
\name{.mm_params}
\alias{.mm_params}
\title{Extract Michaelis-Menten parameters (K and Vmax) from a stats object}
\usage{
.mm_params(stats)
}
\arguments{
\item{stats}{A fitted model whose \code{\link[stats:coef]{stats::coef()}} returns named \code{K} and \code{Vm}
/ \code{Vmax} coefficients (e.g. an \code{nls} fit of \code{v ~ Vm * S / (K + S)}), or a
named numeric vector / list containing those values. \code{NULL} returns
\code{NULL}.}
}
\value{
A named list with elements \code{K} and \code{Vmax} (each possibly \code{NA} when
not found), or \code{NULL} when \code{stats} is \code{NULL}.
}
\description{
Pulls the Michaelis constant (\code{K}) and maximum velocity (\code{Vmax}) from a
fitted model or a supplied set of coefficients, for use in plot annotations.
}
\author{
Jacob Martin
}
\keyword{internal}
